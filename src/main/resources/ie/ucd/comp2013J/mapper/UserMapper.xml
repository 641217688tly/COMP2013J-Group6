<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ie.ucd.comp2013J.mapper.UserMapper">
    <resultMap id="UserResult" type="ie.ucd.comp2013J.pojo.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="role" property="role"/>
    </resultMap>

    <insert id="insert" parameterType="ie.ucd.comp2013J.pojo.User">
        INSERT INTO users (username, password, email, role)
        VALUES (#{username}, #{password}, #{email}, #{role})
    </insert>

    <update id="update" parameterType="ie.ucd.comp2013J.pojo.User">
        UPDATE users
        SET username = #{username},
            password = #{password},
            email    = #{email},
            role     = #{role}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="int" resultMap="UserResult">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="UserResult">
        SELECT * FROM users
    </select>
</mapper>
